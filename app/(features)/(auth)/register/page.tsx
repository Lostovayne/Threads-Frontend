import { FC, ReactElement } from 'react';

interface RegisterPageProps {
  // Prop types here
}

const RegisterPage: FC<RegisterPageProps> = ({}): ReactElement => {
  return (
    <div>
      content
    </div>
  );
};

export default RegisterPage;